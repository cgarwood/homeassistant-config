maintenance:
  name: Maintenance
  view: yes
  entities:
    - group.door_sensor_batteries
    - group.window_sensor_batteries
    - group.smoke_detector_batteries
    - group.system_monitor
    - group.ups
    - group.water_sensors
    - group.all_automations
    - group.ad_automations
    - group.tablet_controls
    
ad_automations:
  name: AppDaemon Automations
  entities:
    - input_boolean.ad_door_chime
    - input_boolean.ad_auto_bathroom_lights
    
tablet_controls:
  name: Tablet Controls
  entities:
    - input_boolean.tb_living_room_quiet
    - input_boolean.tb_master_bedroom_quiet
    - input_boolean.tb_guest_bedroom_quiet
    - input_boolean.tb_nursery_quiet